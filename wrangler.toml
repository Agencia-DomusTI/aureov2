# wrangler.toml
name = 'aureov2'
compatibility_date = '2024-01-01'
pages_build_output_dir = 'dist'

# Requerido para que `npx wrangler deploy` funcione en Cloudflare Builds
[assets]
directory = './dist'
not_found_handling = 'single-page-application'
