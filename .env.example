# ─── Cloudflare Pages (frontend) — valores públicos seguros ───
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Stripe publishable key (pública — visible en browser)
VITE_STRIPE_PB_KEY=pk_live_xxx

# ─── Supabase Edge Functions → Secrets (NUNCA prefijo VITE_) ───
# supabase secrets set GROQ_API_KEY=gsk_xxx
# supabase secrets set GHL_API_TOKEN=pit-xxx
# supabase secrets set GHL_LOCATION_ID=C6shsXK6KDZS3bqsrcMJ
# supabase secrets set STRIPE_SECRET_KEY=sk_live_xxx
# supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_xxx
# supabase secrets set SITE_URL=https://aureoclinique.com
# supabase secrets set ADMIN_JWT_SECRET=una_clave_larga_minimo_32_caracteres
# supabase secrets set GOOGLE_CLIENT_ID=xxx
# supabase secrets set GOOGLE_CLIENT_SECRET=xxx

# ─── Supabase setup ───
# 1. supabase login --token sbp_xxx
# 2. supabase link --project-ref TU-REF  (ya hecho)
# 3. supabase db push --linked
# 4. supabase functions deploy
# 5. Authentication → crear usuario admin

# Webhook de Stripe:
#   URL: https://TU-PROYECTO.supabase.co/functions/v1/stripe-webhook
#   Evento: checkout.session.completed
#
# Google OAuth redirect:
#   https://TU-PROYECTO.supabase.co/functions/v1/admin-google-callback
